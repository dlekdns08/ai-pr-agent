[project]
name = "ai-pr-agent"
version = "0.1.0"
description = "AI-powered code review agent for GitHub (PR + commit)"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=0.92.0",
    "fastapi>=0.135.3",
    "httpx>=0.28.1",
    "pyjwt[crypto]>=2.12.1",
    "uvicorn>=0.44.0",
]
